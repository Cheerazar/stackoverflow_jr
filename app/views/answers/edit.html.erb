<h1>Edit your answer</h1>
<ul>
<%= form_for :answer, url: update_answer_path(@answer.question_id, @answer), method: 'put' do |f| %>
  <li>
    <%= f.label :body %>
    <%= f.text_field :answer, :value => @answer.body %>
  </li>
  <li>
    <%= f.submit %>
  </li>

<% end%>
</ul>
