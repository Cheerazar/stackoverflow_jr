<div id="users-list">
  <h1 class="user-greeting span_4_of_4">USERS</h1>
  <ul>
    <% @users.each do |user| %>
      <li class="user-name">
      <div><%= user.first_name %> <%= user.last_name %> - <%= link_to user.username, user_path(user) %></div>
      </li>
    <% end %>
  </ul>
</div>
