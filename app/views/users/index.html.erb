<div id="users-list">
  <h2>USERS</h2>
  <ul>
    <% @users.each do |user| %>
      <li>
      <div><%= user.first_name %> <%= user.last_name %> - <%= link_to user.username, user_path(user) %></div>
      </li>
    <% end %>
  </ul>
</div>
